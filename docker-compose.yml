services:
  vault:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "3001:3001"
    volumes:
      - ./vault:/vault
    environment:
      GIT_USER_NAME: ${GIT_USER_NAME:-Vault}
      GIT_USER_EMAIL: ${GIT_USER_EMAIL:-vault@localhost}
